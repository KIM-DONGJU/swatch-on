const API_URL = 'https://api.swatchon.com';
const JSON_PATH = '/marketingItems';

const endpoints = {
  marketingItems: `${API_URL}/api/v1/marketing_items`,
  marketingItemsJson: `${JSON_PATH}`
}

export default endpoints;